# Own for Android v2.6.16 紧急修复版 (v2.6.16-hotfix)

* **彻底修复顶部导航栏（Top App Bar）异常变黑与前景色反转**：清理 API 26+ 设备误加载的旧版残留样式，修复 Toolbar 主题属性映射；顶部全面应用 Surface 纯白/深色纯净底色与无边沉浸设计，标题及汉堡菜单、搜索、添加等功能图标在浅色模式下完美呈现高对比深色（`#1C1B1F`），并在暗色模式下自适应切换浅色（`#E6E1E5`）；
* **系统状态栏沉浸适配**：浅色模式下正确同步开启 `windowLightStatusBar`，使顶部电量、时间等状态栏图标呈现深色，与浅色顶栏融为一体；
* **柔化侧边抽屉（Navigation Drawer）未选中图标**：重构导航抽屉状态选择器（ColorStateList），补齐默认状态兜底项，彻底杜绝 fallback 造成的纯黑突兀感；未选中状态图标调整为柔和的 MD3 次要中性灰（`#757575` / `#CAC4D0`），文字保持主要层级字色，选中项保持主题色与胶囊背景高亮。

# Own for Android v2.6.15 预览版 (v2.6.15-preview)

* **多维度二维码分享能力升级**：重构节点二维码弹窗（QRCodeDialog），新增“标准格式”与“通用格式 (SN Link)”分段切换器（TabLayout），支持在标准协议 URI（如 `ss://`, `vmess://`, `vless://` 等）与完整客户端特化链接（`sn://`）间无缝切换，并提供二维码实时重绘、复制当前链接与节点协议 Chip 标识；
* **暗色模式扫码增强**：二维码展示区域特别设计防反转白底圆角容器与呼吸边距，彻底解决暗色模式/深色主题下反色导致其他设备摄像头难以识别的扫码痛点；
* **剪贴板导出多格式分离**：节点操作面板彻底解耦分享动作，细分为“复制标准链接 (Standard Link)”与“复制通用链接 (SN Link)”，并附带直观副标题说明，支持针对不支持标准链接的节点类型智能隐藏不可用选项；
* **文件存储访问框架 (SAF) 配置文件直接导出**：新增“导出为配置文件 (.json / .txt)”功能，接入 Android 原生 Storage Access Framework (`CreateDocument`)，用户可自定义选择存储目录安全保存单节点配置，无需申请敏感的存储读写权限；
* **操作面板层级扁平化与视觉纯净**：保持单层 BottomSheet 扁平布局规范，杜绝多级嵌套菜单，完美继承 MD3 动态主题配色与平滑圆角体系。

# Own for Android v2.6.14 预览版 (v2.6.14-preview)

* **全局 Material Design 3 深度规范落地**：建立沉浸式顶栏规范（消灭实色大面积填充，采用背景沉浸式 Top App Bar，配合滚动抬升与高对比字色），主色使用场景严格克制并聚焦于交互核心；
* **色彩系统与暗色模式精细重塑**：重构浅色与暗色基底，全面引入 Surface Container 规范，消除刺眼对比，实现自定义主题色与系统 Monet 动态配色的无缝平滑适配；
* **设置中心消除生硬分割线**：全局剔除 Preference 传统生硬灰色分割细线，折叠分类全面独立为 16dp 大圆角 Surface 容器，设置 6dp 呼吸感垂直间距；折叠标题未展开状态克制为高对比文字，展开态高亮主题色，统一指示箭头 16dp 右边距；
* **导航抽屉动态胶囊选中态**：侧边栏选中项严格计算应用 12% alpha 主题色胶囊背景与主题色文字，未选中态自动切换为空心轮廓图标（Outlined）与次要字色；
* **操作菜单原生分组化**：主页更多操作等浮层菜单采用标准逻辑分组（功能操作、测速维护、界面布局），引入原生分割视觉线，消除杂乱平铺；
* **节点卡片基线秩序与协议胶囊**：剩余流量与操作按钮实现基线对齐，底部协议类型升级为 12% alpha 背景、6dp 圆角的精致小胶囊 Chip，加粗字号与延迟数值垂直居中基线对齐；
* **分页标签与悬浮按钮质感提升**：TabLayout 背景无缝融入顶栏沉浸层，底部指示器优化为 3dp 并精准跟随主题色，取消全大写英文强制转换；列表底部提供 88dp 呼吸感留白，杜绝 FAB 遮挡。
